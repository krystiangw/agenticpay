{
  "name": "@agenticpay/example-research-server",
  "version": "0.0.1",
  "private": true,
  "description": "Real-world x402 paywalled MCP server template — paid LLM-backed text tools (summarize, extract_entities, sentiment, translate) instead of toy reverse_string",
  "type": "module",
  "scripts": {
    "dev": "tsx --env-file=../../.env src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.107.0",
    "@x402/core": "^2.17.0",
    "@x402/express": "^2.17.0",
    "@x402/svm": "^2.17.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.0.0"
  }
}
