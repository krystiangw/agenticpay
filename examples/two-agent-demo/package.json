{
  "name": "@agentpay/two-agent-demo",
  "version": "0.0.1",
  "private": true,
  "description": "Two-agent demo: agent A pays agent B in USDC for tool calls (x402 + Solana)",
  "type": "module",
  "scripts": {
    "client": "tsx src/agent-client.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agentpay/sdk": "workspace:*",
    "@solana/kit": "^5.5.0",
    "@x402/core": "^2.11.0",
    "@x402/fetch": "^2.11.0",
    "@x402/svm": "^2.11.0"
  }
}
