name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: typecheck + build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # pnpm version is taken from `packageManager` in package.json
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      # Build first so workspace packages emit their dist/ (cli depends on
      # @agentpay/sdk via workspace:*; typecheck cannot resolve it without dist).
      - name: Build
        run: pnpm -r build

      - name: Typecheck
        run: pnpm -r typecheck
